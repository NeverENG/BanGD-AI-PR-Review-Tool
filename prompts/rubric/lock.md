## 锁与隔离级别 (Locking & Isolation)

**触发信号**：事务路径、`Lock/Unlock`、隔离级别、MVCC 版本链、可见性判断。

**该追问的架构问题**
- 锁顺序是否一致？是否引入了潜在死锁（多锁获取顺序不一致）？
- 改动是否破坏了隔离级别保证（脏读/不可重复读/幻读）？MVCC 版本可见性判断是否仍正确？
- 长事务/长持锁是否会阻塞热路径？能否缩短临界区或改为乐观并发？
